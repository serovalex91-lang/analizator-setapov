# Telegram Userbot - Чтение сообщений

Простой бот для чтения всех сообщений из Telegram и сохранения их в файлы.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте конфигурацию в `userbot.py`:
   - Замените `your_api_hash_here` на ваш API Hash
   - Замените `your_phone_number_here` на ваш номер телефона

## Запуск

```bash
python userbot.py
```

При первом запуске потребуется ввести код подтверждения из Telegram.

## Функции

- 📱 Читает все сообщения из всех чатов
- 📝 Сохраняет сообщения в `messages.log` и `messages.csv`
- 👤 Показывает информацию об отправителе
- 🕐 Временные метки в UTC

## Файлы

- `messages.log` - простой лог всех сообщений
- `messages.csv` - структурированные данные в CSV формате 